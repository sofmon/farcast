# AllThing

> AI abstraction — cloud-agnostic layer over managed AI services (Gemini, Claude, OpenAI).

AllThing abstracts cloud AI providers behind a uniform interface. Applications access AI capabilities through the FarCast SDK — never directly through a provider. Initially provides a chat interface via FarSight, evolving into the AI backbone for the entire system.

*Specification and implementation details to follow.*
