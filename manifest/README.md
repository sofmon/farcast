# Manifest Specification

> The `./farcast` manifest — a minimal declaration of what to run.

The manifest describes the application, not its infrastructure. TechnoCore handles resource management automatically.

*Full specification to follow.*
